{
  "name": "powerchord",
  "version": "1.1.0",
  "description": "Katalog chord gitar dan lirik lagu PowerChord",
  "private": true,
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "npm run prepare:public && npx -y serve public -p 8080",
    "start": "npm run prepare:public && npx -y serve public -p 8080",
    "prepare:public": "node scripts/prepare_public.mjs",
    "check": "node --check public/js/main.js && node --check public/js/core.js && node --check scripts/check_static_site.mjs && python3 scripts/validate_data.py && npm run verify:static",
    "pretest": "npm run minify:css",
    "test": "node --test tests/*.test.js",
    "verify:static": "node scripts/check_static_site.mjs",
    "minify:css": "cleancss -o public/css/style.min.css public/css/style.css",
    "build": "npm run prepare:public && npm run minify:css && npm run check && npm test",
    "optimize:assets": "python3 scripts/optimize_assets.py",
    "db:seed": "python3 scripts/seed_database.py"
  },
  "engines": {
    "node": ">=20",
    "python": ">=3.11"
  },
  "keywords": [
    "chord",
    "gitar",
    "lirik",
    "musik"
  ],
  "author": "PowerChord Contributors",
  "license": "ISC",
  "devDependencies": {
    "axe-core": "^4.13.0",
    "clean-css-cli": "^5.6.3"
  }
}
