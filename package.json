{
  "name": "powerchord",
  "version": "1.1.0",
  "description": "Katalog chord gitar dan lirik lagu PowerChord",
  "private": true,
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "npx -y serve . -p 8080",
    "start": "npx -y serve . -p 8080",
    "check": "node --check js/main.js && node --check js/core.js && node --check scripts/check_static_site.mjs && python3 scripts/validate_data.py && npm run verify:static",
    "test": "node --test tests/*.test.js",
    "verify:static": "node scripts/check_static_site.mjs",
    "minify:css": "cleancss -o css/style.min.css css/style.css",
    "build": "npm run minify:css && npm run check && npm test",
    "optimize:assets": "python3 scripts/optimize_assets.py"
  },
  "engines": {
    "node": ">=20",
    "python": ">=3.11"
  },
  "keywords": [
    "chord",
    "gitar",
    "lirik",
    "musik"
  ],
  "author": "PowerChord Contributors",
  "license": "ISC",
  "devDependencies": {
    "axe-core": "^4.13.0",
    "clean-css-cli": "^5.6.3"
  }
}
